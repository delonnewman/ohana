- clients:
  - Sinatra (admin interface)
  - JavaScript (WebSockets)
        - will need to write websockets server as proxy
  - .NET
  - Python
  - Perl
- add message queue and dispatcher process that feeds off it
  - make queue in memory but abstract it so that other adapters can be made
